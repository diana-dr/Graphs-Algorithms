from UI import UI

# 1. Write a program that, given a directed graph and two vertices,
# finds a lowest length path between them, by using a forward breadth-first search from the starting vertex.

ui = UI("input.txt")
ui.run()